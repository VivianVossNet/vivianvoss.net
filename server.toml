[server]
port = 1337
bundle = "vv-website"

[extensions]
enabled = ["contact_form", "highscores", "csrf_token", "robots", "sitemap", "llms", "rss"]

[extensions.routes]
"/contact/send" = "contact_form"
"/api/highscores" = "highscores"
"/robots.txt" = "robots"
"/sitemap.xml" = "sitemap"
"/llms.txt" = "llms"
"/feed" = "rss"

# [mail] — configured on production server (credentials not in repo)
