[server]
port = 1337
bundle = "vv-website"

[extensions]
enabled = ["contact_form", "highscores", "robots", "sitemap", "llms"]

[extensions.routes]
"/contact/send" = "contact_form"
"/api/highscores" = "highscores"
"/robots.txt" = "robots"
"/sitemap.xml" = "sitemap"
"/llms.txt" = "llms"

# [mail] — configured on production server (credentials not in repo)
